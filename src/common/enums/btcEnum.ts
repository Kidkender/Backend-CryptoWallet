export const enum PositionTransfer {
  SENDER = "Sender",
  RECEIVER = "Receiver",
}

export enum LevelRetrieveBlock {
  HEX_DATA = 0,
  BLOCK_HASH = 1,
  WITHOU_PREVOUT = 2,
  WITH_PREVOUT = 3,
}

export enum VERBORSITY {
  HEX_DATA = 0,
  JSON_INFO = 1,
  WITH_PREVOUT = 2,
}
